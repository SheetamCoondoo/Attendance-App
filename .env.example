APP_ENV=production
SECRET_KEY=replace-with-a-long-random-secret
TRUST_PROXY=1

# Optional office network restriction
REQUIRE_OFFICE_NETWORK=0
ALLOWED_SUBNET=

# Optional if you run without nginx reverse proxy
PORT=8000
